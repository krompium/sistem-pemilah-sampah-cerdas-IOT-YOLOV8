# 🔄 System Workflow Diagram

## Complete End-to-End Process

```
┌─────────────────────────────────────────────────────────────────────┐
│                    SISTEM PEMILAH SAMPAH CERDAS                     │
│                  Smart Waste Sorting System - IoT                   │
└─────────────────────────────────────────────────────────────────────┘

┌────────────────┐
│  1. USER       │
│  Buang Sampah  │
│  ke Penadah    │
└────────┬───────┘
         │
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  2. ESP32-CAM (Motion Detection)                                   │
│  📷 esp32cam_motion_capture.ino                                    │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ • Motion detected (frame comparison)                         │  │
│  │ • Delay 1 second (object stable)                             │  │
│  │ • Capture high-quality photo (UXGA)                          │  │
│  │ • LED flash ON (better lighting)                             │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ HTTP POST (WiFi)
         │ Image (JPEG)
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  3. LAPTOP INFERENCE (YOLOv8 AI)                                   │
│  💻 laptop_inference_dual.py                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ • Receive image (Flask server :5000)                         │  │
│  │ • Run YOLOv8m inference                                       │  │
│  │ • Get class: 0=Organik, 1=Anorganik, 2=B3                    │  │
│  │ • Get confidence score                                        │  │
│  │ • Draw bounding boxes                                         │  │
│  │ • Display GUI (OpenCV)                                        │  │
│  │ • Log to CSV file                                             │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ HTTP POST / Serial
         │ {"class": 0, "confidence": 0.95}
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  4. ESP32 MAIN CONTROLLER (Decision & Control)                     │
│  🎛️ esp32_main_controller.ino                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ STEP 1: Receive classification result                        │  │
│  │ STEP 2: Check bin full (ultrasonic sensor)                   │  │
│  │         ┌─── IF FULL ────┐                                   │  │
│  │         │ LED Error ON    │                                   │  │
│  │         │ Blynk notify    │                                   │  │
│  │         │ REJECT waste    │                                   │  │
│  │         └─────────────────┘                                   │  │
│  │         ┌─── IF EMPTY ───┐                                    │  │
│  │         │ Continue...     │                                   │  │
│  │         └─────────────────┘                                   │  │
│  │ STEP 3: Rotate platform servo to target bin                  │  │
│  │         • Organik   → 0°                                      │  │
│  │         • Anorganik → 90°                                     │  │
│  │         • B3        → 180°                                    │  │
│  │ STEP 4: Wait 500ms (platform stable)                         │  │
│  │ STEP 5: Open penadah servo (90°)                             │  │
│  │ STEP 6: Wait 2 seconds (waste falls)                         │  │
│  │ STEP 7: Close penadah servo (0°)                             │  │
│  │ STEP 8: Rotate platform to HOME (0°)                         │  │
│  │ STEP 9: Update counters (organik++/anorganik++/b3++)        │  │
│  │ STEP 10: LED indicator ON (category color)                   │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ WiFi (Blynk API)
         │ Update virtual pins
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  5. BLYNK IOT DASHBOARD (Monitoring)                               │
│  ☁️ blynk_dashboard.py (optional sync)                             │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ • V0: Counter Organik                                        │  │
│  │ • V1: Counter Anorganik                                      │  │
│  │ • V2: Counter B3                                             │  │
│  │ • V3: Total Processed                                        │  │
│  │ • V4: System Status ("READY")                                │  │
│  │ • V5: Bin Status (JSON)                                      │  │
│  │ • V7: Reset Button                                           │  │
│  │ • 🔔 Push notification if bin full                           │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────┘

         ↓
    ✅ COMPLETE!
    System ready for next waste item
```

---

## Hardware Components Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        HARDWARE LAYOUT                          │
└─────────────────────────────────────────────────────────────────┘

                     ESP32-CAM (Top View)
                           📷
                           │
                           ↓
                  ┌────────────────┐
                  │   PENADAH      │  ← Temporary container
                  │   SEMENTARA    │     User drops waste here
                  └────────┬───────┘
                           │
                    Servo Penadah (GPIO 12)
                    Open/Close mechanism
                           │
                           ↓
                  ┌────────────────┐
                  │   PLATFORM     │  ← Rotating platform
                  │   ROTASI       │
                  └────────┬───────┘
                           │
                    Servo Platform (GPIO 13)
                    Rotates to 3 positions
                           │
              ┌────────────┼────────────┐
              │            │            │
              ↓            ↓            ↓
         ┌────────┐   ┌────────┐   ┌────────┐
         │  BIN   │   │  BIN   │   │  BIN   │
         │ORGANIK │   │ANORGAN.│   │  B3    │
         │   0°   │   │  90°   │   │  180°  │
         └────────┘   └────────┘   └────────┘
             ↑            ↑            ↑
             │            │            │
        [HC-SR04]    [HC-SR04]    [HC-SR04]  ← Bin full sensors
        GPIO 26/27   GPIO 14/25   GPIO 32/33

                    ESP32 DevKit V1
                    Main Controller
                    ┌──────────┐
                    │ GPIO 18  │ → LED Organik (Green)
                    │ GPIO 19  │ → LED Anorganik (Blue)
                    │ GPIO 21  │ → LED B3 (Red)
                    │ GPIO 22  │ → LED System (White)
                    │ GPIO 23  │ → LED Error (Yellow)
                    └──────────┘
```

---

## Communication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    COMMUNICATION ARCHITECTURE                    │
└─────────────────────────────────────────────────────────────────┘

    ESP32-CAM              LAPTOP              ESP32 Main
    (Camera)            (Inference)           (Controller)
        │                    │                     │
        │  ① Image (WiFi)    │                     │
        │───────────────────→│                     │
        │   HTTP POST        │                     │
        │   :80/upload       │                     │
        │                    │                     │
        │                    │ ② Result (WiFi)     │
        │                    │────────────────────→│
        │                    │   HTTP POST         │
        │                    │   :80/classify      │
        │                    │                     │
        │                    │ ③ Fallback (Serial) │
        │                    │────────────────────→│
        │                    │   115200 baud       │
        │                    │   (if WiFi fails)   │
        │                    │                     │
        │                    │                     │
        │                    │  ④ Status (WiFi)    │
        │                    │←────────────────────│
        │                    │   HTTP GET          │
        │                    │   :80/status        │
        │                    │                     │
        ↓                    ↓                     ↓
                        ☁️ BLYNK CLOUD
                        (IoT Dashboard)
                        Virtual Pins V0-V7
                        Push Notifications
```

---

## File Dependencies

```
┌─────────────────────────────────────────────────────────────────┐
│                      FILE DEPENDENCIES                          │
└─────────────────────────────────────────────────────────────────┘

config/config.example.py
    │
    ├─→ inference/laptop_inference_dual.py
    │       │
    │       ├─→ models/best.pt (YOLOv8 model)
    │       ├─→ logs/waste_sorting.csv (output)
    │       └─→ captured_images/*.jpg (output)
    │
    └─→ inference/blynk_dashboard.py
            │
            └─→ Communicates with ESP32 Main

esp32/esp32_main_controller/esp32_main_controller.ino
    │
    ├─→ Requires WiFi credentials
    ├─→ Requires Blynk Auth Token
    └─→ Communicates with laptop & Blynk

esp32/esp32cam_motion_capture/esp32cam_motion_capture.ino
    │
    ├─→ Requires WiFi credentials
    ├─→ Requires Laptop IP address
    └─→ Sends images to laptop

docs/*.md
    │
    └─→ User guides and references
```

---

## Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         DATA FLOW                               │
└─────────────────────────────────────────────────────────────────┘

Waste → ESP32-CAM → Image (JPEG) → Laptop → YOLOv8
                                      ↓
                                  Class ID (0/1/2)
                                  Confidence (0.0-1.0)
                                      ↓
                                  ESP32 Main
                                      ↓
                    ┌─────────────────┼─────────────────┐
                    ↓                 ↓                 ↓
              Counter Update    Servo Control    LED Control
                    ↓                 ↓                 ↓
               Blynk Sync      Physical Sorting   Visual Feedback
                    ↓                 ↓                 ↓
              Dashboard         Bin Selection      User sees LED
              Update            Waste sorted        (Green/Blue/Red)
                    ↓
              CSV Log
              captured_images/
```

---

## Timing Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                      TIMING DIAGRAM                             │
└─────────────────────────────────────────────────────────────────┘

Time   Action                                Duration
─────  ──────────────────────────────────  ────────
0.0s   User drops waste                    -
0.1s   Motion detected                     -
1.1s   Capture photo (after 1s delay)      ~0.2s
1.3s   Send to laptop via WiFi             ~0.3s
1.6s   YOLOv8 inference on laptop          ~0.5s
2.1s   Send result to ESP32                ~0.1s
2.2s   Check bin full (ultrasonic)         ~0.1s
2.3s   Rotate platform to position         ~0.5s
2.8s   Wait for platform stable            0.5s
3.3s   Open penadah                        ~0.3s
3.6s   Wait for waste to fall              2.0s
5.6s   Close penadah                       ~0.3s
5.9s   Platform back to HOME               ~0.5s
6.4s   Update counters & Blynk             ~0.2s
6.6s   LED indicator                       2.0s
8.6s   Ready for next waste                -

TOTAL: ~8.6 seconds per waste item
(Can be optimized to ~3-5 seconds)
```

---

## System States

```
┌─────────────────────────────────────────────────────────────────┐
│                      SYSTEM STATES                              │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│ INITIALIZING │  Power on, WiFi connect, Blynk connect
└──────┬───────┘
       │
       ↓
┌──────────────┐
│   READY      │  Idle, waiting for motion detection
└──────┬───────┘
       │
       ↓ (Motion detected)
┌──────────────┐
│  CAPTURING   │  ESP32-CAM taking photo
└──────┬───────┘
       │
       ↓
┌──────────────┐
│  INFERENCING │  Laptop running YOLOv8
└──────┬───────┘
       │
       ↓
┌──────────────┐
│  CHECKING    │  ESP32 checking bin full
└──────┬───────┘
       │
       ├─→ (Bin FULL) ──→ ERROR state
       │
       ↓ (Bin OK)
┌──────────────┐
│  SORTING     │  Servos moving, waste being sorted
└──────┬───────┘
       │
       ↓
┌──────────────┐
│  UPDATING    │  Update counters, Blynk, LEDs
└──────┬───────┘
       │
       ↓
┌──────────────┐
│   READY      │  Back to idle, ready for next
└──────────────┘

ERROR state:
    └─→ LED Error ON → Blynk notify → Wait user → READY
```

---

## Success Criteria Checklist

✅ Motion detection triggers capture
✅ Image sent to laptop successfully (WiFi)
✅ YOLOv8 inference runs (<2 seconds)
✅ Correct classification (>70% confidence)
✅ Bin full check works (ultrasonic accurate)
✅ Platform rotates to correct bin
✅ Penadah opens/closes smoothly
✅ Waste falls into correct bin (100% accuracy after calibration)
✅ Counters update correctly
✅ Blynk dashboard shows real-time data
✅ LEDs indicate correct category
✅ CSV log created with all data
✅ System ready for next waste (<10 seconds)

---

**For detailed setup, see**: `docs/QUICK_START_GUIDE.md`
